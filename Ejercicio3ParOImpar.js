let numero = parseInt(prompt('Ingresa un número para ver si es par o impar:'));

if (numero % 2 === 0) {
  console.log(`El número ${numero} es par`);
} else {
  console.log(`El número ${numero} es impar`);
}